{% extends 'base.html.twig' %}

{% block body %}

    <div class ="container">

        {#{% include 'header.html.twig' %}#}
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}

    </div>
{% endblock %}